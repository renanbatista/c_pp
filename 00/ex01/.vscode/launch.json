{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch with LLDB",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/MyAwesomePhoneBook",
            "args": [""],
            "preLaunchTask": "clang build",
            "cwd": "${workspaceFolder}"
        }
    ]
}